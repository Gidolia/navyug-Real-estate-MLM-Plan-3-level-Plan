<?php
include "config.php";
/*function total_team()
{
    global $con;
    $r=0;
    $s="SELECT * FROM `distributor` WHERE `s_id`='$_SESSION[d_id]'";
    $rs=$con->query($s);
    while($ss=$rs->fetch_assoc())
    {
        $r++;
    }
    return $r;
}
    */
?>